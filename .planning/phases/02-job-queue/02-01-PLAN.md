---
phase: 02-job-queue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
  - backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
  - backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py
  - backend/packages/fgc_flow_api/tests/test_job_models.py
autonomous: true
requirements:
  - JOB-01
  - JOB-07
must_haves:
  truths:
    - "Queued jobs persist in SQLite with immutable model_version_id references."
    - "Cached results are keyed by a stable SHA-256 hash of sorted parameters plus model_version_id."
    - "The jobs package exports the new ORM models without import ambiguity."
  artifacts:
    - path: backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
      provides: Job ORM model for durable queue state
    - path: backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
      provides: CachedResult ORM model for deterministic result caching
    - path: backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py
      provides: barrel exports for job models
    - path: backend/packages/fgc_flow_api/tests/test_job_models.py
      provides: model-shape regression tests
  key_links:
    - from: backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
      to: backend/packages/fgc_flow_api/fgc_flow_api/database.py
      via: JobsBase subclassing
      pattern: 'class Job\(JobsBase\)'
    - from: backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
      to: backend/packages/fgc_flow_api/fgc_flow_api/database.py
      via: JobsBase subclassing
      pattern: 'class CachedResult\(JobsBase\)'
---

<objective>
Create the durable job/cached-result tables for the SQLite queue.

Purpose: make the queue state authoritative in SQLite and pin each job to an immutable model version per D-02, D-05, and D-06.
Output: Job/CachedResult ORM models, package exports, and regression tests.
</objective>

<execution_context>
@/home/aadil/Documents/github/fgc-studio/.planning/PROJECT.md
@/home/aadil/Documents/github/fgc-studio/.planning/ROADMAP.md
@/home/aadil/Documents/github/fgc-studio/.planning/STATE.md
@/home/aadil/Documents/github/fgc-studio/.planning/phases/02-job-queue/02-CONTEXT.md
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/database.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/exceptions.py
@/home/aadil/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aadil/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
From `backend/packages/fgc_flow_api/fgc_flow_api/database.py`:
```python
class JobsBase(DeclarativeBase):
    pass

async def get_jobs_db() -> AsyncSession
async def init_jobs_db()
```

From `backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py`:
```python
from fgc_core.models.user import User
```

From Phase 2 locked context:
- D-02: SQLite DB records are the source of truth.
- D-05: Uploaded models are immutable version records.
- D-06: Jobs reference model version IDs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the job table shape in tests</name>
  <files>backend/packages/fgc_flow_api/tests/test_job_models.py</files>
  <read_first>backend/packages/fgc_flow_api/fgc_flow_api/database.py, backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py, backend/packages/fgc_flow_api/tests/conftest.py</read_first>
  <behavior>
    - Test `Job` exposes id, user_id, job_type, model_version_id, status, params, status_events, result_path, retry_count, next_retry_at, created_at, started_at, completed_at, and error columns.
    - Test `CachedResult` exposes model_version_id, params_hash, result_json/result_path, and created_at columns with a uniqueness rule on the cache key.
    - Test the package barrel can import the new models without side effects.
  </behavior>
  <action>Write regression tests that assert the SQLite jobs schema is durable and version-pinned per D-02/D-05/D-06. The tests must fail until the ORM models exist, and they must check the exact column names and unique cache-key constraint.</action>
  <acceptance_criteria>
    - `pytest tests/test_job_models.py -q` is the verification target for the task.
    - The test file asserts the exact Job and CachedResult column sets.
    - The test file asserts the cached-result key is unique for a given model_version_id + params_hash pair.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_models.py -q</automated>
  </verify>
  <done>Schema expectations are encoded in tests and the queue contract is fixed to SQLite source-of-truth semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Job and CachedResult ORM models</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/models/job.py, backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py, backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py</files>
  <read_first>backend/packages/fgc_flow_api/tests/test_job_models.py, backend/packages/fgc_flow_api/fgc_flow_api/database.py, backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py</read_first>
  <behavior>
    - Job stores queue state in SQLite with immutable model_version_id, status history, retry count, timestamps, and result/error pointers.
    - CachedResult stores the canonical result for a model_version_id + params_hash pair and can be queried without in-memory state.
    - Imports stay absolute and the models remain attached to JobsBase only.
  </behavior>
  <action>Implement `Job` and `CachedResult` as `JobsBase` ORM classes. Use a string UUID primary key, require `model_version_id` on every job row, keep `params` JSON-serializable, and store retry/status history durably in SQLite so later plans can poll and resume from DB state per D-02, D-05, and D-06. Export the classes from `models/__init__.py`.</action>
  <acceptance_criteria>
    - `Job.__table__.columns.keys()` includes the required queue fields plus `model_version_id`.
    - `CachedResult.__table__.columns.keys()` includes `model_version_id` and `params_hash`.
    - `models.__all__` exports the new ORM classes.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_models.py -q</automated>
  </verify>
  <done>Durable queue tables exist and the package exports them cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate the schema against the jobs DB</name>
  <files>backend/packages/fgc_flow_api/tests/test_job_models.py, backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py</files>
  <read_first>backend/packages/fgc_flow_api/fgc_flow_api/database.py, backend/packages/fgc_flow_api/fgc_flow_api/models/job.py, backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py</read_first>
  <behavior>
    - The jobs DB creates both tables from JobsBase metadata.
    - Importing the barrel does not instantiate any queue worker or trigger inline execution.
    - The schema remains the sole source of truth for queue persistence.
  </behavior>
  <action>Run a metadata and import sanity pass after the model implementation is in place. Confirm the SQLite schema is created from `JobsBase.metadata`, the package exports remain stable, and nothing introduces an in-memory queue shortcut against D-01 or D-02.</action>
  <acceptance_criteria>
    - The test command exits 0.
    - The metadata check sees both `job` and `cached_result` tables.
    - No task introduces synchronous job execution in this phase.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_models.py -q</automated>
  </verify>
  <done>The queue persistence contract is validated end-to-end against SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → jobs API | Untrusted job params, model_version_id, and polling identifiers enter here |
| jobs API → SQLite | Job rows and cached results must be validated before persistence/readback |
| worker → filesystem | Result file pointers are trusted only after DB validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | model validation | mitigate | Require strict Pydantic/ORM fields for model_version_id and canonical params serialization so submitted queue data cannot mutate the stored contract. |
| T-02-02 | I | jobs DB tables | mitigate | Keep SQLite as the authoritative queue store and avoid in-memory mirrors so status, retries, and results cannot drift. |
| T-02-03 | D | jobs schema | accept | The queue tables are local-only SQLite records; lock contention is handled in later plans by WAL armor already present in database.py. |
</threat_model>

<verification>
- `pytest tests/test_job_models.py -q` passes.
- `Job` and `CachedResult` import from `fgc_flow_api.models`.
- `JobsBase.metadata` contains both queue tables.
</verification>

<success_criteria>
- Queue persistence is modeled in SQLite.
- Job rows include immutable model_version_id references.
- Cached results are addressable by stable cache key.
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-queue/02-01-SUMMARY.md`
</output>

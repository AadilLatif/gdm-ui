---
phase: 02-job-queue
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py
  - backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py
  - backend/packages/fgc_flow_api/fgc_flow_api/main.py
  - backend/packages/fgc_flow_api/tests/test_job_cache_retry.py
autonomous: true
requirements:
  - JOB-06
  - JOB-07
must_haves:
  truths:
    - "Identical job submissions for the same model_version_id and sorted params reuse a cached SQLite result."
    - "Failed jobs retry exactly three times with exponential backoff before terminal failure."
    - "The FastAPI app boots the queue router and worker from lifespan without any inline execution shortcut."
  artifacts:
    - path: backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py
      provides: SHA-256 cache key normalization and cached-result lookups
    - path: backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py
      provides: fixed 3-attempt retry/backoff policy
    - path: backend/packages/fgc_flow_api/fgc_flow_api/main.py
      provides: router inclusion and worker lifespan wiring
    - path: backend/packages/fgc_flow_api/tests/test_job_cache_retry.py
      provides: cache/retry integration tests
  key_links:
    - from: backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py
      to: backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
      via: cached-result persistence
      pattern: 'params_hash'
    - from: backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py
      to: backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
      via: retry_count and next_retry_at updates
      pattern: 'retry_count'
    - from: backend/packages/fgc_flow_api/fgc_flow_api/main.py
      to: backend/packages/fgc_flow_api/fgc_flow_api/routers/jobs.py
      via: app.include_router
      pattern: 'include_router\(jobs_router\)'
---

<objective>
Finish cache deduplication, retry policy, and app boot wiring for the job queue.

Purpose: make cached results deterministic, cap failures at three retries, and start the worker/router from FastAPI lifespan per D-01 through D-04.
Output: cache and retry services, main-app wiring, and integration tests.
</objective>

<execution_context>
@/home/aadil/Documents/github/fgc-studio/.planning/PROJECT.md
@/home/aadil/Documents/github/fgc-studio/.planning/ROADMAP.md
@/home/aadil/Documents/github/fgc-studio/.planning/STATE.md
@/home/aadil/Documents/github/fgc-studio/.planning/phases/02-job-queue/02-CONTEXT.md
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/main.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/routers/jobs.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/services/job_worker.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
@/home/aadil/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aadil/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
From `backend/packages/fgc_flow_api/fgc_flow_api/main.py`:
```python
@asynccontextmanager
async def lifespan(app: FastAPI): ...
```

From the Phase 2 locked context:
- D-01: Always queue simulation jobs.
- D-02: SQLite DB records are the source of truth.
- D-03: Retry policy is fixed at 3 attempts.
- D-04: Results are stored in SQLite records with file pointers as needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock cache and retry behavior in tests</name>
  <files>backend/packages/fgc_flow_api/tests/test_job_cache_retry.py</files>
  <read_first>backend/packages/fgc_flow_api/fgc_flow_api/models/job.py, backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py, backend/packages/fgc_flow_api/tests/conftest.py</read_first>
  <behavior>
    - Canonical hashing sorts params before hashing and includes model_version_id in the key.
    - A cache hit returns the same stored result for identical inputs.
    - Retry delays grow exponentially and stop after the third failure.
  </behavior>
  <action>Write integration tests that pin down the deterministic cache key and the exact three-attempt retry policy per D-03 and D-04. The tests must prove that equivalent parameter sets map to the same cache key and that retries stop after the fixed bound.</action>
  <acceptance_criteria>
    - The cache test asserts SHA-256 canonicalization over sorted params.
    - The retry test asserts exactly three attempts before terminal failure.
    - The tests do not rely on in-memory cache state as the source of truth.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_cache_retry.py -q</automated>
  </verify>
  <done>Cache and retry rules are encoded in tests before wiring changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cache and retry services</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py, backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py</files>
  <read_first>backend/packages/fgc_flow_api/tests/test_job_cache_retry.py, backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py, backend/packages/fgc_flow_api/fgc_flow_api/models/job.py</read_first>
  <behavior>
    - Cache keys are built from model_version_id + canonical JSON of sorted params.
    - CachedResult rows are read and written through SQLite only.
    - Retry scheduling uses a fixed three-attempt policy with exponential backoff and records the next retry time on the job row.
  </behavior>
  <action>Implement `job_cache.py` and `job_retry.py` as explicit service modules. The cache module must canonicalize params, compute the SHA-256 key, and upsert `CachedResult` rows; the retry module must compute the backoff schedule, increment retry_count, and persist the next retry timestamp. Keep the policy fixed at three attempts per D-03 and store results durably per D-04.</action>
  <acceptance_criteria>
    - `build_cache_key(...)` is deterministic for equal logical inputs.
    - `should_retry(...)` returns false after the third failure.
    - The cache service can look up and persist cached results in the jobs DB.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_cache_retry.py -q</automated>
  </verify>
  <done>The cache and retry policy are implemented as SQLite-backed services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire cache, retry, router, and worker into app startup</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/main.py, backend/packages/fgc_flow_api/tests/test_job_cache_retry.py</files>
  <read_first>backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py, backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py, backend/packages/fgc_flow_api/fgc_flow_api/services/job_worker.py, backend/packages/fgc_flow_api/fgc_flow_api/routers/jobs.py, backend/packages/fgc_flow_api/fgc_flow_api/routers/__init__.py</read_first>
  <behavior>
    - FastAPI lifespan initializes the jobs DB, mounts the jobs router, and starts the worker with cache/retry collaborators.
    - The app never falls back to inline simulation execution.
    - Startup/shutdown leaves the queue state in SQLite only.
  </behavior>
  <action>Update `main.py` so the app boot path includes the jobs router and launches the worker with the cache and retry collaborators from this phase. Keep the worker loop behind lifespan management and preserve D-01 by never adding a synchronous fallback path.</action>
  <acceptance_criteria>
    - `app.include_router(jobs_router)` is present in main.py.
    - The lifespan path starts and stops the worker cleanly.
    - Job submission remains queued even after cache/retry wiring exists.
  </acceptance_criteria>
  <verify>
    <automated>cd backend/packages/fgc_flow_api &amp;&amp; pytest tests/test_job_cache_retry.py -q</automated>
  </verify>
  <done>The queue cache/retry path is wired into the app without reintroducing inline execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache key → SQLite | Canonical hash inputs must be normalized before reuse decisions |
| retry policy → worker | Retry scheduling must not become unbounded or user-configurable |
| app lifespan → background worker | Startup/shutdown must not leak background tasks or bypass DB state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | T | cache lookup | mitigate | Canonicalize params before hashing so equivalent inputs cannot be used to poison or bypass cached results. |
| T-02-11 | D | retry scheduler | mitigate | Hard-code the three-attempt policy and exponential backoff so retries cannot grow without bound. |
| T-02-12 | I | app startup | mitigate | Wire the worker through FastAPI lifespan so the queue stays observable in SQLite and the app does not spawn stray in-memory execution paths. |
</threat_model>

<verification>
- `pytest tests/test_job_cache_retry.py -q` passes.
- `main.py` includes `jobs_router` and worker lifespan wiring.
- Identical params + model_version_id reuse the same cached result.
</verification>

<success_criteria>
- Cache hits are deterministic.
- Retry stops after three failures.
- The app boot path includes the queue router and worker without inline execution.
</success_criteria>

<output>
After completion, create `.planning/phases/02-job-queue/02-04-SUMMARY.md`
</output>

---
phase: 03-core-simulation-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/packages/fgc_flow_api/fgc_flow_api/database.py
  - backend/packages/fgc_flow_api/fgc_flow_api/models/model.py
  - backend/packages/fgc_flow_api/fgc_flow_api/schemas/models.py
  - backend/packages/fgc_flow_api/fgc_flow_api/services/model_service.py
  - backend/packages/fgc_flow_api/fgc_flow_api/routers/models.py
  - backend/packages/fgc_flow_api/tests/test_model_contracts.py
  - backend/packages/fgc_flow_api/tests/test_model_upload_list.py
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "User can upload a GDM model file and receive a model_id with saved metadata"
    - "User can list only their own uploaded models with name, ID, upload date, and file size"
    - "Uploaded model files are stored under a user-scoped directory and are not written outside the upload root"
    - "Invalid or unsafe model archives are rejected before any DB row is committed"
  artifacts:
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/database.py"
      provides: "flow metadata database engine/session for uploaded models"
      min_lines: 60
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/models/model.py"
      provides: "uploaded model ORM table"
      min_lines: 25
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/schemas/models.py"
      provides: "model upload/list request and response contracts"
      min_lines: 25
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/routers/models.py"
      provides: "POST /api/models/upload and GET /api/models"
      min_lines: 40
  key_links:
    - from: "routers/models.py"
      to: "services/model_service.py"
      via: "Depends(get_flow_db) + get_current_user"
      pattern: 'Depends\(get_flow_db\).*Depends\(get_current_user\)'
    - from: "services/model_service.py"
      to: "fgc_core.services.file_service.handle_zip_upload"
      via: "zip extraction and path validation"
      pattern: 'handle_zip_upload\('
    - from: "routers/models.py"
      to: "database.py"
      via: "async flow DB session"
      pattern: 'async def get_flow_db\('
---

<objective>
Build the uploaded-model foundation for Phase 3: a dedicated flow metadata DB, model ORM/schema contracts, and safe upload/list endpoints.

Purpose: simulations need persisted model records before solver endpoints can resolve model_id to a real GDM file.
Output: flow DB/session helpers, model metadata ORM, upload/list service, model router, and contract tests.
</objective>

<execution_context>
@/home/aadil/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aadil/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/database.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/config.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/routers/jobs.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_core/fgc_core/routers/projects.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_core/fgc_core/services/file_service.py

<interfaces>
From backend/packages/fgc_flow_api/fgc_flow_api/config.py:
```python
class Settings(BaseSettings):
    app_name: str = "FGC Flow API"
    debug: bool = False
    base_dir: Path = Path(__file__).resolve().parent.parent
    upload_dir: Path | None = None
    database_url: str | None = None
    jobs_database_url: str | None = None
```

From backend/packages/fgc_flow_api/fgc_flow_api/database.py:
```python
class JobsBase(DeclarativeBase):
    pass

async def get_jobs_db() -> AsyncSession: ...
async def init_jobs_db(): ...
```

From backend/packages/fgc_core/fgc_core/routers/projects.py:
```python
@router.post("/upload", response_model=ProjectResponse, status_code=status.HTTP_201_CREATED)
async def upload_model(
    file: UploadFile = File(...),
    name: str = "",
    description: str = "",
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    ...
```

From backend/packages/fgc_core/fgc_core/services/file_service.py:
```python
def handle_zip_upload(file_content: bytes, original_filename: str, user_id: str) -> tuple[str, str]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flow metadata DB and model contracts</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/database.py, backend/packages/fgc_flow_api/fgc_flow_api/models/model.py, backend/packages/fgc_flow_api/fgc_flow_api/schemas/models.py, backend/packages/fgc_flow_api/tests/test_model_contracts.py</files>
  <read_first>
    - backend/packages/fgc_flow_api/fgc_flow_api/database.py
    - backend/packages/fgc_flow_api/fgc_flow_api/config.py
    - backend/packages/fgc_core/fgc_core/routers/projects.py
    - backend/packages/fgc_core/fgc_core/services/file_service.py
  </read_first>
  <action>Add a separate flow metadata persistence layer in `database.py`: create `FlowBase`, `flow_engine`, `FlowAsyncSession`, `get_flow_db()`, and `init_flow_db()` using `settings.database_url` with the same WAL/busy_timeout/foreign_keys PRAGMAs already used for the jobs DB. Define `Model` in `models/model.py` with `id`, `user_id`, `name`, `file_path`, `file_size`, and `created_at`. Define Pydantic contracts in `schemas/models.py` for upload input plus list/upload responses. Add a contract test file that asserts the ORM table name, required columns, and schema fields. Do not change the existing jobs DB behavior.</action>
  <acceptance_criteria>
    - `database.py` contains `FlowBase` and `init_flow_db()`
    - `models/model.py` contains `__tablename__ = "model"`
    - `schemas/models.py` exports upload/list response models with `model_id`, `name`, `file_size`, and `created_at`
    - `pytest tests/test_model_contracts.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_model_contracts.py -q</automated>
  </verify>
  <done>The model metadata DB and its contracts exist and are locked by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe model upload and listing</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/services/model_service.py, backend/packages/fgc_flow_api/fgc_flow_api/routers/models.py, backend/packages/fgc_flow_api/tests/test_model_upload_list.py</files>
  <read_first>
    - backend/packages/fgc_flow_api/fgc_flow_api/models/model.py
    - backend/packages/fgc_flow_api/fgc_flow_api/schemas/models.py
    - backend/packages/fgc_flow_api/fgc_flow_api/database.py
    - backend/packages/fgc_core/fgc_core/services/file_service.py
    - backend/packages/fgc_core/fgc_core/routers/projects.py
  </read_first>
  <action>Implement `POST /api/models/upload` and `GET /api/models`. Use `get_current_user` and `get_flow_db()` for auth and persistence. Accept multipart `file` plus optional `name`; reject non-`.zip` uploads, reject archives that fail `handle_zip_upload()`, persist the record in the flow DB, and store files under `settings.upload_dir / user.id / model_id`. `GET /api/models` must filter by the authenticated user only and return `model_id`, `name`, `created_at`, and `file_size` for each row. Keep the router standalone-testable so the task can mount it in a temporary FastAPI app without wiring `main.py` yet.</action>
  <acceptance_criteria>
    - `routers/models.py` defines `POST /api/models/upload` and `GET /api/models`
    - invalid zip/path-traversal uploads return 400 and create no DB row
    - model listing is filtered by `user.id`
    - `pytest tests/test_model_upload_list.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_model_upload_list.py -q</automated>
  </verify>
  <done>Uploaded models can be stored and listed safely, with ownership filtering and archive validation enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → upload endpoint | untrusted multipart zip and model name input |
| API → filesystem | extracted GDM files written under the upload root |
| API → flow DB | model metadata persistence and per-user listing queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | upload handler / file_service | mitigate | reject `.zip` only, validate extracted member paths, and remove temp dirs on failure before DB commit |
| T-03-01-02 | I | models list query | mitigate | require `get_current_user` and filter every query by `user_id == user.id` |
| T-03-01-03 | D | upload path / archive extraction | mitigate | enforce size checks and fail fast on invalid archives to avoid disk exhaustion |
</threat_model>

<verification>
- Contract tests for the flow DB and model schema pass
- Upload/list router tests pass
- No unsafe path writes are possible from the upload route
</verification>

<success_criteria>
- Uploaded models are persisted in the flow DB
- Users only see their own models
- Unsafe or invalid uploads are rejected without partial persistence
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-simulation-endpoints/03-01-SUMMARY.md`
</output>

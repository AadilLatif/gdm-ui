---
phase: 03-core-simulation-endpoints
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - backend/packages/fgc_flow_api/fgc_flow_api/services/solver_runner.py
  - backend/packages/fgc_flow_api/fgc_flow_api/services/batch_jobs.py
  - backend/packages/fgc_flow_api/fgc_flow_api/routers/simulations.py
  - backend/packages/fgc_flow_api/tests/test_simulation_compare_batch.py
autonomous: true
requirements:
  - SIM-04
  - SIM-05
must_haves:
  truths:
    - "User can run compare to see AC OPF, DC OPF, and LinDistFlow results side by side for the same model"
    - "User can submit a batch parameter sweep and receive queued job ids for every sweep point"
    - "Compare results are deterministic and reuse the same normalization path as single-solver runs"
    - "Batch requests validate the sweep grid before any job is created"
  artifacts:
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/services/batch_jobs.py"
      provides: "batch sweep expansion and job creation helpers"
      min_lines: 60
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/routers/simulations.py"
      provides: "POST /api/simulations/compare and /api/simulations/batch"
      min_lines: 110
    - path: "backend/packages/fgc_flow_api/tests/test_simulation_compare_batch.py"
      provides: "compare and batch behavior tests"
      min_lines: 50
  key_links:
    - from: "routers/simulations.py"
      to: "services/solver_runner.py"
      via: "shared solver execution helper"
      pattern: "run_compare|run_single_solver"
    - from: "services/batch_jobs.py"
      to: "fgc_flow_api.models.Job"
      via: "batch sweep job creation"
      pattern: 'Job\(.*job_type=.*batch'
    - from: "services/batch_jobs.py"
      to: "fgc_flow_api.database.get_jobs_db"
      via: "queue persistence"
      pattern: 'get_jobs_db\('
---

<objective>
Add the Phase 3 compare and batch endpoints on top of the single-solver implementation.

Purpose: compare must reuse the exact single-solver serialization path; batch must fan out into the existing job queue instead of inventing a second async system.
Output: compare orchestration, batch job helpers, router endpoints, and focused behavior tests.
</objective>

<execution_context>
@/home/aadil/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aadil/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/services/solver_runner.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/routers/simulations.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/database.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py
@/home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py
@/home/aadil/Documents/github/fgc-flow/src/fgc_flow/mcp/server.py

<interfaces>
From backend/packages/fgc_flow_api/fgc_flow_api/models/job.py:
```python
class Job(JobsBase):
    id: Mapped[str]
    user_id: Mapped[str]
    job_type: Mapped[str]
    model_version_id: Mapped[str]
    status: Mapped[str]
    params: Mapped[dict]
    status_events: Mapped[list]
    result_path: Mapped[str | None]
    retry_count: Mapped[int]
```

From backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py:
```python
def build_cache_key(model_version_id: str, params: dict) -> str: ...
async def get_cached_result(db: AsyncSession, model_version_id: str, params: dict) -> CachedResult | None: ...
```

From /home/aadil/Documents/github/fgc-flow/src/fgc_flow/mcp/server.py:
```python
async def _handle_compare_solvers(args: dict[str, Any]) -> dict[str, Any]:
    ...
    return {
        "ac": ac_summary,
        "dc": dc_summary,
        "lindistflow": ldf_summary,
        "summary": {...},
    }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend solver runner for compare and batch fan-out</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/services/solver_runner.py, backend/packages/fgc_flow_api/fgc_flow_api/services/batch_jobs.py, backend/packages/fgc_flow_api/tests/test_simulation_compare_batch.py</files>
  <read_first>
    - backend/packages/fgc_flow_api/fgc_flow_api/services/solver_runner.py
    - backend/packages/fgc_flow_api/fgc_flow_api/models/job.py
    - backend/packages/fgc_flow_api/fgc_flow_api/models/cached_result.py
    - backend/packages/fgc_flow_api/fgc_flow_api/services/job_cache.py
    - backend/packages/fgc_flow_api/fgc_flow_api/services/job_retry.py
    - /home/aadil/Documents/github/fgc-flow/src/fgc_flow/mcp/server.py
  </read_first>
  <action>Add `run_compare()` so it executes AC OPF, DC OPF, and LinDistFlow against the same loaded model and returns a side-by-side summary using the same serializer helpers as single-solver runs. Add batch helpers that expand a `parameter_grid` cartesian product, validate every sweep point up front, and create one `Job` row per sweep point with `model_version_id` set to the uploaded model UUID and `params` containing the exact sweep values plus solver config. Keep cache-key generation canonical so repeated identical sweeps can reuse results later.</action>
  <acceptance_criteria>
    - compare helper returns `ac`, `dc`, `lindistflow`, and `summary`
    - batch helper creates one queued job per sweep point
    - identical sweep inputs generate the same cache key
    - `pytest tests/test_simulation_compare_batch.py -q` passes after the helper layer is complete
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_simulation_compare_batch.py -q</automated>
  </verify>
  <done>Compare and batch orchestration helpers exist and are deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Add compare and batch endpoints</name>
  <files>backend/packages/fgc_flow_api/fgc_flow_api/routers/simulations.py, backend/packages/fgc_flow_api/tests/test_simulation_compare_batch.py</files>
  <read_first>
    - backend/packages/fgc_flow_api/fgc_flow_api/routers/simulations.py
    - backend/packages/fgc_flow_api/fgc_flow_api/schemas/simulations.py
    - backend/packages/fgc_flow_api/fgc_flow_api/services/batch_jobs.py
    - backend/packages/fgc_flow_api/fgc_flow_api/services/solver_runner.py
  </read_first>
  <action>Add `POST /api/simulations/compare` and `POST /api/simulations/batch`. Compare should call the shared `run_compare()` helper and return the normalized side-by-side payload. Batch should validate the sweep grid, submit one job per sweep point via the jobs DB helper, and return job ids plus sweep metadata. Keep the response envelope consistent: compare is inline, batch is queued, and neither should mutate the single-solver contract.</action>
  <acceptance_criteria>
    - `routers/simulations.py` defines `/compare` and `/batch`
    - compare returns the same normalized solver shape as single-solver endpoints
    - batch returns queued job ids and rejected invalid sweep grids
    - `pytest tests/test_simulation_compare_batch.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_simulation_compare_batch.py -q</automated>
  </verify>
  <done>Compare and batch endpoints are available and use the shared orchestration helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → compare/batch endpoints | untrusted grid sizes and solver config combinations |
| API → jobs DB | queue rows created for every sweep point |
| API → solver runtime | repeated solver executions can amplify compute cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | D | batch endpoint | mitigate | validate the sweep grid before queueing and cap the number of sweep combinations per request |
| T-03-03-02 | T | compare serialization | mitigate | reuse the single-solver serializer path so compare cannot drift into a different schema |
| T-03-03-03 | I | queued batch jobs | mitigate | persist user ownership in each job row and require authenticated job creation |
</threat_model>

<verification>
- Compare endpoint returns side-by-side solver summaries
- Batch endpoint enqueues one job per sweep point
- Invalid sweep grids are rejected before queue writes
</verification>

<success_criteria>
- Compare and batch modes are available on the simulation router
- Compare output is deterministic and normalized
- Batch uses the existing job queue, not a separate execution path
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-simulation-endpoints/03-03-SUMMARY.md`
</output>

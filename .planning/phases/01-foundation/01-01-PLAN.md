---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/packages/fgc_flow_api/pyproject.toml
  - backend/packages/fgc_flow_api/fgc_flow_api/__init__.py
  - backend/packages/fgc_flow_api/fgc_flow_api/config.py
  - backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py
  - backend/packages/fgc_flow_api/fgc_flow_api/routers/__init__.py
  - backend/packages/fgc_flow_api/fgc_flow_api/schemas/__init__.py
  - backend/packages/fgc_flow_api/fgc_flow_api/services/__init__.py
  - backend/packages/fgc_flow_api/tests/__init__.py
  - backend/packages/fgc_flow_api/tests/conftest.py
autonomous: true
requirements: [FND-01, FND-02, FND-03]
must_haves:
  truths:
    - "Package fgc_flow_api is installable via `pip install -e .`"
    - "Package imports from fgc_core without errors"
    - "Package lists fgc-core and fgc-flow as dependencies in pyproject.toml"
    - "Package structure mirrors fgc_core conventions for consistency"
  artifacts:
    - path: "backend/packages/fgc_flow_api/pyproject.toml"
      provides: "Package metadata and dependencies"
      min_lines: 25
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/__init__.py"
      provides: "Package entry point"
      contains: '__version__'
    - path: "backend/packages/fgc_flow_api/fgc_flow_api/config.py"
      provides: "Settings class"
      contains: "class Settings(BaseSettings)"
  key_links:
    - from: "fgc_flow_api/pyproject.toml"
      to: "fgc_core"
      via: "dependency on fgc-core"
      pattern: "fgc-core"
    - from: "fgc_flow_api/pyproject.toml"
      to: "fgc-flow"
      via: "dependency on fgc-flow"
      pattern: "fgc-flow"
    - from: "fgc_flow_api/config.py"
      to: "fgc_core/config.py"
      via: "imports Settings pattern"
      pattern: "pydantic_settings.BaseSettings"
---

<objective>
Create the `fgc_flow_api` package scaffold with `pyproject.toml`, directory structure, and empty module files ready for implementation.

**Purpose:** Establish the package foundation so subsequent plans can add database, auth, and app code. The structure mirrors `fgc_core`'s conventions for monorepo consistency.

**Output:** Runnable Python package at `backend/packages/fgc_flow_api/` with all directories, `pyproject.toml`, and empty `__init__.py` files.
</objective>

<execution_context>
@/home/aadil/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aadil/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@backend/packages/fgc_core/pyproject.toml
@backend/packages/fgc_core/fgc_core/config.py
@backend/pyproject.toml

<interfaces>
<!-- Existing patterns from fgc_core to replicate -->

From backend/packages/fgc_core/fgc_core/config.py:
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    app_name: str = "FGC Flow API"
    # ...fields with model_config = {"env_prefix": "FGC_FLOW_", ...}

settings = Settings()
```

From backend/packages/fgc_core/pyproject.toml:
```toml
[project]
name = "fgc-core"
dependencies = ["fastapi>=0.115.0", "uvicorn[standard]>=0.32.0", ...]
```

From backend/pyproject.toml:
```toml
[tool.uv.workspace]
members = ["packages/*"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and directory structure</name>
  <files>
    backend/packages/fgc_flow_api/pyproject.toml
    backend/packages/fgc_flow_api/fgc_flow_api/__init__.py
    backend/packages/fgc_flow_api/fgc_flow_api/config.py
    backend/packages/fgc_flow_api/fgc_flow_api/models/__init__.py
    backend/packages/fgc_flow_api/fgc_flow_api/routers/__init__.py
    backend/packages/fgc_flow_api/fgc_flow_api/schemas/__init__.py
    backend/packages/fgc_flow_api/fgc_flow_api/services/__init__.py
    backend/packages/fgc_flow_api/tests/__init__.py
    backend/packages/fgc_flow_api/tests/conftest.py
  </files>
  <read_first>
    backend/packages/fgc_core/pyproject.toml — copy dependency style, version, dev conventions
    backend/packages/fgc_core/fgc_core/config.py — copy Settings pattern (pydantic_settings.BaseSettings)
    backend/pyproject.toml — workspace member config already exists
  </read_first>
  <action>
    Create the full directory structure for `fgc_flow_api`:

    ```
    backend/packages/fgc_flow_api/
    ├── pyproject.toml
    ├── fgc_flow_api/
    │   ├── __init__.py
    │   ├── config.py
    │   ├── models/
    │   │   └── __init__.py
    │   ├── routers/
    │   │   └── __init__.py
    │   ├── schemas/
    │   │   └── __init__.py
    │   └── services/
    │       └── __init__.py
    └── tests/
        ├── __init__.py
        └── conftest.py
    ```

    **pyproject.toml:**
    ```toml
    [build-system]
    requires = ["setuptools>=69", "wheel"]
    build-backend = "setuptools.build_meta"

    [project]
    name = "fgc-flow-api"
    version = "0.1.0"
    description = "REST API for FGC Flow power flow simulations"
    readme = "README.md"
    requires-python = ">=3.12"
    license = { text = "MIT" }
    dependencies = [
        "fgc-core",                              # sibling workspace package - imports auth, DB, config
        "fgc-flow>=0.1.0",                        # power flow solver library
        "fastapi>=0.115.0",
        "uvicorn[standard]>=0.32.0",
        "sqlalchemy>=2.0",
        "greenlet>=3.0",
        "aiosqlite>=0.20.0",
        "python-jose[cryptography]>=3.3.0",
        "bcrypt>=4.0",
        "python-multipart>=0.0.12",
        "pydantic>=2.0",
        "pydantic-settings>=2.0",
        "email-validator>=2.0",
    ]

    [project.optional-dependencies]
    dev = [
        "ruff>=0.11.0",
        "pytest>=8.0",
        "pytest-asyncio>=0.24.0",
    ]

    [tool.uv]
    package = true
    ```

    **`fgc_flow_api/__init__.py`:**
    ```python
    """FGC Flow API — RESTful power flow simulation backend."""
    __version__ = "0.1.0"
    ```

    **`fgc_flow_api/config.py`:**
    ```python
    from pathlib import Path

    from pydantic_settings import BaseSettings


    class Settings(BaseSettings):
        app_name: str = "FGC Flow API"
        debug: bool = False

        # Paths
        base_dir: Path = Path(__file__).resolve().parent.parent
        upload_dir: Path | None = None
        database_url: str | None = None
        jobs_database_url: str | None = None

        # Auth — imported from fgc_core settings for consistency
        # fgc_core's secret_key is used for JWT signing
        access_token_expire_minutes: int = 60
        refresh_token_expire_days: int = 7

        # CORS
        cors_origins: list[str] = [
            "http://localhost:5173",
            "http://localhost:3000",
            "http://localhost:8080",
        ]

        model_config = {"env_prefix": "FGC_FLOW_", "env_file": base_dir / ".env"}


    settings = Settings()
    if settings.upload_dir is None:
        settings.upload_dir = Path.home() / "Documents" / "gfc_files" / "uploads"
    if settings.database_url is None:
        settings.database_url = "sqlite+aiosqlite:///" + str(
            Path.home() / "Documents" / "gfc_files" / "database" / "fgc_flow.db"
        )
    if settings.jobs_database_url is None:
        settings.jobs_database_url = "sqlite+aiosqlite:///" + str(
            Path.home() / "Documents" / "gfc_files" / "database" / "fgc_flow_jobs.db"
        )
    settings.upload_dir.mkdir(parents=True, exist_ok=True)
    ```

    **All `__init__.py` files (models/, routers/, schemas/, services/, tests/):** Empty files.

    **`tests/conftest.py`:**
    ```python
    """Shared test fixtures for fgc_flow_api."""
    ```
  </action>
  <acceptance_criteria>
    - `backend/packages/fgc_flow_api/pyproject.toml` exists with `name = "fgc-flow-api"`
    - `backend/packages/fgc_flow_api/fgc_flow_api/__init__.py` exists with `__version__`
    - `backend/packages/fgc_flow_api/fgc_flow_api/config.py` exists with `class Settings(BaseSettings)`
    - All subdirectories have `__init__.py` files
    - pyproject.toml has both `fgc-core` and `fgc-flow` in dependencies
    - pyproject.toml has `[tool.uv] package = true`
  </acceptance_criteria>
  <verify>
    <automated>ls backend/packages/fgc_flow_api/fgc_flow_api/config.py && grep -q "class Settings" backend/packages/fgc_flow_api/fgc_flow_api/config.py && grep -q "fgc-core" backend/packages/fgc_flow_api/pyproject.toml && grep -q "fgc-flow" backend/packages/fgc_flow_api/pyproject.toml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - All scaffold files exist at correct paths
    - pyproject.toml is valid TOML with correct dependencies
    - config.py has Settings class with env_prefix=FGC_FLOW_
  </done>
</task>

<task type="auto">
  <name>Task 2: Create conftest.py with basic infrastructure and verify install</name>
  <files>
    backend/packages/fgc_flow_api/tests/conftest.py
  </files>
  <read_first>
    backend/packages/fgc_core/tests/conftest.py — copy test patterns
    backend/packages/fgc_core/pyproject.toml — verify dev dependency pattern
  </read_first>
  <action>
    Expand `tests/conftest.py` with:
    ```python
    """Shared test fixtures for fgc_flow_api."""
    import pytest


    @pytest.fixture
    def app():
        """Return a FastAPI test client instance.
        
        Set by the test configuration when the app module is implemented.
        Currently a placeholder — will be populated in plan 01-04 IMPLEMENTATION.
        """
        return None
    ```

    Then verify the install works:
    ```bash
    cd backend/packages/fgc_flow_api && pip install -e . --quiet 2>&1 && python -c "import fgc_flow_api; print(fgc_flow_api.__version__)" && echo "INSTALL_OK" || echo "INSTALL_FAILED"
    ```
    If the install fails (e.g., fgc-flow not available), note in the output but do NOT block — fgc-flow is optional until Phase 3. The core install without fgc-flow should succeed.
  </action>
  <acceptance_criteria>
    - `tests/conftest.py` contains `@pytest.fixture` and `def app():`
    - `import fgc_flow_api` succeeds from the package directory
    - `fgc_flow_api.__version__` returns `"0.1.0"`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aadil/Documents/github/fgc-studio/backend/packages/fgc_flow_api && python -c "import fgc_flow_api; print(fgc_flow_api.__version__)" 2>&1 | grep -q "0.1.0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Package is importable and __version__ is correct
    - conftest.py has basic test fixture structure
  </done>
</task>

</tasks>

<verification>
1. All scaffold files exist: `ls -R backend/packages/fgc_flow_api/`
2. Package installs: `cd backend/packages/fgc_flow_api && python -c "import fgc_flow_api"`
3. Config has correct Settings class with env_prefix
4. pyproject.toml has all required dependencies
</verification>

<success_criteria>
- Package scaffold is committed and installable
- All __init__.py files exist in required directories
- config.py is ready for downstream plans to import Settings
- No fgc_core code duplicated — only imports planned
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
